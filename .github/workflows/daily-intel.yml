name: Daily e-mobility intelligence

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/daily-intel.yml"
      - "config/**"
      - "scripts/daily_intel.mjs"
      - "templates/intel_dashboard.html"
  schedule:
    # 23:30 UTC is 07:30 in Taipei (UTC+8) on the following calendar day.
    - cron: "30 23 * * *"

permissions:
  contents: write

concurrency:
  group: daily-e-mobility-intel
  cancel-in-progress: false

jobs:
  collect:
    runs-on: ubuntu-latest
    env:
      # The secret is optional. When absent, the collector still runs and keeps
      # the original title/summary; when set, it writes concise Traditional Chinese briefs.
      OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
      OPENAI_MODEL: gpt-5-mini

    steps:
      - name: Check out the workspace
        uses: actions/checkout@v4

      - name: Use Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Collect new intelligence
        shell: bash
        run: |
          if [ -f state/seen_items.json ]; then
            node scripts/daily_intel.mjs --ai-summary-limit 28
          else
            node scripts/daily_intel.mjs --baseline --ai-summary-limit 28
          fi

      - name: Save reports and deduplication state
        shell: bash
        run: |
          git add daily-output state "資訊工作台.html"
          if git diff --cached --quiet; then
            echo "No new report files to commit."
          else
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git commit -m "chore: update daily e-mobility intelligence"
            git push
          fi
